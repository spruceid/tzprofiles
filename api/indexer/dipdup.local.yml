database:
  kind: postgres
  host: 127.0.0.1
  port: 5432
  user: dipdup
  password: ${POSTGRES_PASSWORD:-changeme}
  database: dipdup

hasura:
  url: http://127.0.0.1:8080
  admin_secret: ${ADMIN_SECRET:-changeme}
