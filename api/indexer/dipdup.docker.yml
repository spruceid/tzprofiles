database:
  kind: postgres
  host: db
  port: 5432
  user: dipdup
  password: ${POSTGRES_PASSWORD:-changeme}
  database: dipdup

hasura:
  url: http://hasura:8080
  admin_secret: ${HASURA_SECRET:-changeme}

sentry:
  dsn: ${SENTRY_DSN:-""}
  environment: ${SENTRY_ENVIRONMENT:-docker}

prometheus:
  host: 0.0.0.0